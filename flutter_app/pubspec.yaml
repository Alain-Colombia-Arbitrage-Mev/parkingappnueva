name: flutter_app
description: "Parkiing - Subastas de servicios y ofertas flash"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # State management
  flutter_riverpod: ^3.0.3
  
  # Navigation
  go_router: ^17.0.0
  
  # Backend
  convex_flutter: ^1.2.0
  
  # Auth
  auth0_flutter: ^1.7.2
  
  # Storage
  flutter_secure_storage: ^9.2.2
  
  # UI
  flutter_animate: ^4.5.2
  
  # Maps
  google_maps_flutter: ^2.10.0
  
  # Files
  image_picker: ^1.1.2
  file_picker: ^10.3.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
